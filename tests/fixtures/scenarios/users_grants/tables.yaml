tables:
  - name: users
    columns:
      - name: user_id
        type: integer
        primary_key: true
      - name: user_name
        type: text
        not_null: true
      - name: status
        type: text
        default: active
